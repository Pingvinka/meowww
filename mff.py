import torch
from transformers import AutoModel, AutoTokenizer
import numpy as np
from sklearn.metrics.pairwise import cosine_similarity
import json
from typing import List, Dict, Tuple
import warnings
warnings.filterwarnings('ignore')

class JinaEmbedder:
    """Класс для работы с Jina Embeddings"""
    
    def __init__(self, model_name="jinaai/jina-embeddings-v2-base-en"):
        self.tokenizer = AutoTokenizer.from_pretrained(model_name)
        self.model = AutoModel.from_pretrained(model_name)
        self.model.eval()
    
    def get_embeddings(self, texts: List[str]) -> np.ndarray:
        """Получение эмбеддингов для списка текстов"""
        embeddings = []
        
        for text in texts:
            encoded_input = self.tokenizer(
                text, 
                padding=True, 
                truncation=True, 
                return_tensors='pt',
                max_length=512
            )
            
            with torch.no_grad():
                model_output = self.model(**encoded_input)
                # Берем embedding [CLS] токена
                text_embedding = model_output.last_hidden_state[:, 0, :].cpu().numpy()
                embeddings.append(text_embedding[0])
        
        return np.array(embeddings)

class VectorStore:
    """Простая векторная база данных"""
    
    def __init__(self):
        self.documents = []
        self.embeddings = None
        self.metadata = []
    
    def add_documents(self, documents: List[str], metadata: List[Dict] = None):
        """Добавление документов в базу"""
        self.documents.extend(documents)
        
        if metadata:
            self.metadata.extend(metadata)
        else:
            self.metadata.extend([{}] * len(documents))
    
    def build_index(self, embedder: JinaEmbedder):
        """Построение векторного индекса"""
        print("Строим векторный индекс...")
        self.embeddings = embedder.get_embeddings(self.documents)
        print(f"Индекс построен. Размерность: {self.embeddings.shape}")
    
    def search(self, query_embedding: np.ndarray, top_k: int = 3) -> List[Tuple[str, float, Dict]]:
        """Поиск похожих документов"""
        if self.embeddings is None:
            raise ValueError("Сначала нужно построить индекс!")
        
        # Вычисляем косинусное сходство
        similarities = cosine_similarity([query_embedding], self.embeddings)[0]
        
        # Получаем топ-K результатов
        top_indices = np.argsort(similarities)[::-1][:top_k]
        
        results = []
        for idx in top_indices:
            results.append((
                self.documents[idx],
                similarities[idx],
                self.metadata[idx]
            ))
        
        return results

class RAGSystem:
    """Полная RAG система"""
    
    def __init__(self, embedder_model: str = "jinaai/jina-embeddings-v2-base-en"):
        self.embedder = JinaEmbedder(embedder_model)
        self.vector_store = VectorStore()
        self.setup_demo_data()
    
    def setup_demo_data(self):
        """Настройка демонстрационных данных"""
        
        # Демонстрационные данные о искусственном интеллекте
        documents = [
            # Технологии ИИ
            "Машинное обучение - это область искусственного интеллекта, которая позволяет компьютерам обучаться на данных без явного программирования.",
            "Глубокое обучение использует нейронные сети с множеством слоев для обработки сложных данных таких как изображения и текст.",
            "Трансформеры - это архитектура нейронных сетей, которая революционизировала обработку естественного языка.",
            "GPT-4 является крупной языковой моделью способной генерировать человекообразный текст и решать сложные задачи.",
            
            # RAG техники
            "Retrieval-Augmented Generation (RAG) сочетает поиск информации и генерацию текста для создания точных ответов.",
            "RAG системы сначала находят релевантные документы в базе знаний, а затем используют их для генерации ответа.",
            "Векторные базы данных хранят эмбеддинги текстов для эффективного семантического поиска.",
            "Эмбеддинги - это числовые представления текста, которые захватывают семантическое значение.",
            
            # Приложения
            "ИИ используется в чат-ботах, виртуальных ассистентах и системах рекомендаций.",
            "Компьютерное зрение позволяет машинам распознавать и анализировать изображения и видео.",
            "Обработка естественного языка (NLP) позволяет компьютерам понимать и генерировать человеческий язык.",
            "Автономные автомобили используют ИИ для навигации и принятия решений в реальном времени.",
            
            # Jina Embeddings
            "Jina Embeddings - это модели для создания векторных представлений текста с поддержкой множества языков.",
            "Jina Embeddings v2 поддерживает длину контекста до 8192 токенов и эффективна для задач поиска.",
            "Эмбеддинги Jina оптимизированы для работы в RAG системах и семантического поиска.",
            "MRL технология в Jina позволяет использовать эмбеддинги разной размерности для оптимизации производительности."
        ]
        
        metadata = [
            {"category": "AI технологии", "source": "demo_data"},
            {"category": "AI технологии", "source": "demo_data"},
            {"category": "AI архитектуры", "source": "demo_data"},
            {"category": "AI модели", "source": "demo_data"},
            {"category": "RAG", "source": "demo_data"},
            {"category": "RAG", "source": "demo_data"},
            {"category": "Векторные БД", "source": "demo_data"},
            {"category": "Эмбеддинги", "source": "demo_data"},
            {"category": "Приложения", "source": "demo_data"},
            {"category": "Приложения", "source": "demo_data"},
            {"category": "NLP", "source": "demo_data"},
            {"category": "Автономные системы", "source": "demo_data"},
            {"category": "Jina", "source": "demo_data"},
            {"category": "Jina", "source": "demo_data"},
            {"category": "Jina", "source": "demo_data"},
            {"category": "Jina", "source": "demo_data"}
        ]
        
        self.vector_store.add_documents(documents, metadata)
        self.vector_store.build_index(self.embedder)
        print("✅ Демонстрационные данные загружены и проиндексированы!")
    
    def retrieve(self, query: str, top_k: int = 3) -> List[Tuple[str, float, Dict]]:
        """Поиск релевантных документов"""
        query_embedding = self.embedder.get_embeddings([query])[0]
        results = self.vector_store.search(query_embedding, top_k)
        return results
    
    def generate_prompt(self, query: str, context_docs: List[Tuple[str, float, Dict]]) -> str:
        """Генерация промпта для LLM"""
        context_text = "\n\n".join([f"📄 Документ {i+1} (сходство: {score:.3f}): {doc}" 
                                  for i, (doc, score, metadata) in enumerate(context_docs)])
        
        prompt = f"""Используй приведенные ниже документы, чтобы точно ответить на вопрос пользователя.

КОНТЕКСТ:
{context_text}

ВОПРОС ПОЛЬЗОВАТЕЛЯ: {query}

ИНСТРУКЦИИ:
- Ответь максимально точно на основе предоставленного контекста
- Если в контексте нет информации для ответа, скажи об этом
- Будь конкретен и используй информацию из документов
- Сохраняй профессиональный тон

ОТВЕТ:"""
        return prompt
    
    def mock_llm_generate(self, prompt: str) -> str:
        """
        Упрощенная имитация LLM (в реальном сценарии здесь был бы вызов GPT-4 или подобной модели)
        """
        # В реальной системе здесь был бы вызов к API LLM
        return f"""На основе предоставленного контекста я могу ответить на ваш вопрос.

Основываясь на найденных документах, вот что я могу сказать:

Документы описывают различные аспекты искусственного интеллекта, включая машинное обучение, глубокое обучение и архитектуры трансформеров. 
Jina Embeddings представлены как эффективное решение для создания векторных представлений текста в RAG системах.

Это демонстрационный ответ, показывающий работу RAG системы. В реальном сценарии здесь был бы развернутый ответ от полноценной LLM модели."""

    def ask(self, question: str, top_k: int = 3, verbose: bool = True) -> str:
        """Основной метод для вопросов к RAG системе"""
        if verbose:
            print(f"🧠 Вопрос: {question}")
            print("🔍 Ищу релевантные документы...")
        
        # Этап 1: Retrieval
        relevant_docs = self.retrieve(question, top_k)
        
        if verbose:
            print(f"✅ Найдено {len(relevant_docs)} релевантных документов:")
            for i, (doc, score, metadata) in enumerate(relevant_docs):
                print(f"   {i+1}. [Сходство: {score:.3f}] {doc[:80]}...")
            print("🤖 Генерирую ответ...")
        
        # Этап 2: Augmentation + Generation
        prompt = self.generate_prompt(question, relevant_docs)
        answer = self.mock_llm_generate(prompt)
        
        if verbose:
            print("=" * 80)
            print(f"💬 Ответ: {answer}")
            print("=" * 80)
        
        return {
            "question": question,
            "answer": answer,
            "relevant_documents": [
                {
                    "content": doc,
                    "similarity_score": float(score),
                    "metadata": metadata
                } for doc, score, metadata in relevant_docs
            ]
        }

def main():
    """Демонстрация работы RAG системы"""
    print("🚀 Загружаю RAG систему с Jina Embeddings...")
    
    # Инициализация системы
    rag_system = RAGSystem()
    
    # Демонстрационные вопросы
    demo_questions = [
        "Что такое Jina Embeddings?",
        "Как работает RAG?",
        "Что такое машинное обучение?",
        "Какие применения у искусственного интеллекта?"
    ]
    
    print("\n" + "=" * 60)
    print("ДЕМОНСТРАЦИЯ RAG СИСТЕМЫ")
    print("=" * 60)
    
    # Тестирование системы
    for i, question in enumerate(demo_questions, 1):
        print(f"\n📋 Пример {i}:")
        result = rag_system.ask(question)
        
        # Небольшая пауза между запросами
        if i < len(demo_questions):
            print("\n" + "-" * 60)

if __name__ == "__main__":
    main()
